# FocusSense ML Pipeline Dependencies
# Recommended Python Version: 3.10 or 3.11 (for Mac Silicon compatibility)

# --- Core ML ---
# Numpy 2.0 이상은 아직 호환성 문제가 많으므로 2.0 미만으로 고정하는 것이 안전합니다.
numpy>=1.24.0,<2.0.0
torch>=2.0.0
torchvision>=0.15.0

# --- Computer Vision ---
opencv-python>=4.8.0
mediapipe>=0.10.0
# dlib 설치 전 'brew install cmake' 및 'xcode-select --install' 필수
dlib>=19.24.0

# --- Data Processing ---
pandas>=2.0.0
scikit-learn>=1.3.0
pillow>=10.0.0

# --- CoreML Conversion ---
coremltools>=7.0
onnx>=1.14.0
# onnx-simplifier는 설치 에러가 잦아, 필요할 때 주석 해제 후 설치 권장
# onnx-simplifier>=0.4.0

# --- Training Utilities ---
tqdm>=4.65.0
tensorboard>=2.13.0
matplotlib>=3.7.0
seaborn>=0.12.0

# --- Dataset Management ---
albumentations>=1.3.0
kaggle>=1.5.0

# --- Jupyter ---
jupyter>=1.0.0
ipywidgets>=8.0.0

# --- Code Quality ---
black>=23.0.0
flake8>=6.0.0